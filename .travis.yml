language:
  - swift
osx_image: xcode12.5
script:
  - xcodebuild -scheme Na zdrowie -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 11,OS=14.0' build test
after_success:
  - bash <(curl -s https://codecov.io/bash)
